import { theme } from "@/theme";

export const Badge = theme.styled("span", {
  borderRadius: "500px",
  backgroundColor: "white",
  padding: "$s",
  boxShadow: "rgba(149, 157, 165, 0.2) 0px 8px 24px",
});
